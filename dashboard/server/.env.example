DATABRICKS_SERVER_HOSTNAME=dbc-xxxxxxxx-xxxx.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/xxxxxxxxxxxxxxxx
DATABRICKS_TOKEN=dapi_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DATABRICKS_CATALOG=job_market
DATABRICKS_SCHEMA=gold

PORT=4000

CACHE_TTL_SECONDS=3600

# "Ask the data" chat (Claude API key)
API_CLAUDE_KEY=sk-ant-xxxxxxxxxxxxxxxx

# Optional: read-only Databricks token for the MCP run_sql tool
# (SELECT-only on the gold/silver schemas). Falls back to DATABRICKS_TOKEN.
MCP_DATABRICKS_TOKEN=
